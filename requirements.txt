# LLM / API
openai>=1.30.0
python-dotenv

# Vector store
chromadb>=0.5.0

# Embeddings fallback (used when Vultr not yet configured)
sentence-transformers>=3.0.0

# Financial data
yfinance
pandas

# HTTP
requests

# UI
streamlit>=1.35.0
